name: Demo Action

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: checkout repo
          uses: actions/checkout@v4  
        
        - name: welcome message
          run: echo "My demo action!"

        - name: lsit files
          run: ls

        - name: read file
          run: cat README.md

        - name: test command
          run: ping google.com && nslookup google.com

